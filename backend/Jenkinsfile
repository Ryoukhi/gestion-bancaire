pipeline {
    agent any

    environment {
        DOCKER_IMAGE = "krieger747/core-banking-app"
        DOCKER_TAG = "${env.BUILD_NUMBER}"
        DOCKER_CREDENTIALS = 'dockerhub-credentials'
        BACKEND_DIR = 'backend'
    }

    tools {
        maven 'Maven3'
        jdk 'JDK17'
    }

    stages {

        // L'étape Checkout automatique de Jenkins remplace avantageusement celle-ci

        stage('Build') {
            steps {
                echo 'Compilation du projet...'
                dir(BACKEND_DIR) {
                    bat 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Tests unitaires') {
            steps {
                echo 'Exécution des tests...'
                dir(BACKEND_DIR) {
                    bat 'mvn clean verify'
                }
            }
            post {
                always {
                    junit "${BACKEND_DIR}/**/target/surefire-reports/*.xml"
                }
            }
        }

        stage('Analyse SonarQube') {
            steps {
                echo 'Analyse de la qualité du code...'
                dir(BACKEND_DIR) {
                        script {
                            try {
                                echo "--- TENTATIVE D'ANALYSE SONARQUBE ---"
                                withSonarQubeEnv('SonarQube') {
                                    bat 'mvn sonar:sonar ' +
                                        '-Dsonar.projectKey=gestion-bancaire ' +
                                        '-Dsonar.host.url=http://127.0.0.1:9000 ' +
                                        '-Dsonar.login=sqp_4f6491eb9eceb046118ef43fc7c48396f162ccf5 ' +
                                        '-Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml'
                                }
                            } catch (Exception e) {
                                echo "⚠️ L'analyse SonarQube a échoué (serveur injoignable ou vide), mais on continue le pipeline..."
                                currentBuild.result = 'SUCCESS' // On empêche le build d'échouer ici
                            }
                        }
                }
            }
        }

        stage('Quality Gate') {
            steps {
                echo 'Vérification du Quality Gate...'
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Construction de l\'image Docker...'
                dir(BACKEND_DIR) {
                    script {
                        echo "Building image: ${DOCKER_IMAGE}:${DOCKER_TAG}"
                        
                        // Vérifier que le Dockerfile existe
                        bat 'if not exist Dockerfile (echo ERROR: Dockerfile not found && exit 1)'
                        
                        // Build l'image
                        bat "docker build -t ${DOCKER_IMAGE}:${DOCKER_TAG} ."
                        
                        // Tag comme latest
                        bat "docker tag ${DOCKER_IMAGE}:${DOCKER_TAG} ${DOCKER_IMAGE}:latest"
                        
                        // Vérifier que l'image a été créée
                        bat "docker images ${DOCKER_IMAGE}"
                        
                        echo "✅ Image Docker créée avec succès"
                    }
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo '🚀 Push de l\'image vers Docker Hub...'
                script {
                    withCredentials([usernamePassword(credentialsId: DOCKER_CREDENTIALS,
                            usernameVariable: 'DOCKER_USER',
                            passwordVariable: 'DOCKER_PASS')]) {
                        
                        bat "docker login -u %DOCKER_USER% -p %DOCKER_PASS%"
                        
                        echo "Pushing ${DOCKER_IMAGE}:${DOCKER_TAG}..."
                        bat "docker push ${DOCKER_IMAGE}:${DOCKER_TAG}"
                        
                        echo "Pushing ${DOCKER_IMAGE}:latest..."
                        bat "docker push ${DOCKER_IMAGE}:latest"
                        
                        echo "✅ Images poussées avec succès sur Docker Hub"
                    }
                }
            }
        }

        stage('Clean up') {
            steps {
                echo 'Nettoyage des images locales...'
                script {
                    bat "docker rmi ${DOCKER_IMAGE}:${DOCKER_TAG} || exit 0"
                    bat "docker rmi ${DOCKER_IMAGE}:latest || exit 0"
                }
            }
        }
    }

    post {
        success {
            echo 'Pipeline exécuté avec succès !'
        }
        failure {
            echo 'Le pipeline a échoué.'
        }
        always {
            cleanWs()
        }
    }
}